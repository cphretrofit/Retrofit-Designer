# Orthograph — PAS 2035 Retrofit Design Platform (PRD)

## Product
Premium PAS 2035 retrofit design platform producing audit-ready, site-specific design PDF packs.
Stack: React + FastAPI + MongoDB. PDF via WeasyPrint. AI/Vision via Claude Sonnet 4.6 (Emergent LLM Key). LibreOffice for xlsx→PDF. Google Solar API (user key).

## Core requirements
- Automated site-specific PDF pack generation (WeasyPrint).
- AI-traced SVG floor plans (hallway carving, room tags, measure highlights, door undercut lines).
- Dynamic product datasheets, auto-superseding default brands, purge of AI-guessed products.
- Action items workflow in-UI; Ventilation-first retrofit ordering.
- 3D interactive + PDF floor plans with photo-vision roof massing.
- Universal "Add photo" picker exposing every image embedded across uploaded PDFs.

## Recent changes
- 2026-06: Floor-plan window placement — windows/bays snap to and render on the ACTUAL building perimeter (tight bounds of the rooms union) instead of the outer size-guide envelope, in BOTH the editor (`FloorPlanGeometryEditor.jsx`: handlePlace, window render, drag clamp use bx0/by0/bx1/by1) and the issued PDF (`cad_floorplan.py` window loop anchors to `_bx0.._by1`). Backward-compatible when rooms fill the envelope. Verified via SVG render. OUTSTANDING (issue 2): FA/HLP/P·L data box is still the AI-extracted SAP figure, not recomputed from drawn geometry (extension not counted) — awaiting user decision (compliance-sensitive).
- 2026-06: Photo picker surfaces unified — SiteConditions, Defects, MeasureEvidence pickers share one full-screen surface (z-[70], sticky header inside scroll container, Done pill, grouped sticky labels). Root cause of the confined/half-screen overlay fixed: `.anim-in` used `animation-fill-mode: both`, leaving a lingering `transform` that became the containing block for `position:fixed`; changed to `backwards` so fixed overlays cover the true viewport (verified 1920×1064).
- 2026-06: PDF ventilation section merged into one flowing "Ventilation Strategy & ADF1 Compliance" page (fixes DMEV split/whitespace). Photographic Schedule reflowed from rigid 6-per-page to one flowing grid (~12/page, break-inside:avoid). Pack 113→109 pages.
- 2026-06: Live readiness/completion sync + Photos library sticky Save&Exit + thumbnail lazy-load + Design Pack instant-export cache + LibreOffice conversion cache + uniform @page margins (see earlier entries).
- 2026-06: Photographic Schedule — logos and document scans are now excluded from the Section 06 Photographic Schedule (and cover/evidence selection). New module helper `_is_nonphoto_img` (pdf_builder.py) matches captions/urls containing logo/letterhead/EPC/certificate/floor plan/datasheet/etc.; the schedule filters `photo_uris` through it regardless of the photo's `included` flag, so entries like "Saffron Housing Trust logo placeholder" / "CPH Retrofit company logo" never render in the schedule. The brand mark on the hero/cover is unaffected (it's an HTML overlay, not a photo). Replaced the old nested `_is_doc_img`.
- 2026-06: PDF pack pagination/spacing — replaced the rigid "one .page = one sheet with min-height:285mm + padding-top:18mm" model (top spacing came from CSS padding, which is dropped on continuation pages → content cut off at the top edge across the pack). Now vertical spacing comes from uniform `@page { margin: 16mm 0 16mm 0 }` applied to EVERY physical sheet (incl. overflow/continuation), footer in the bottom margin box; `.page` supplies only the 18mm side gutters for print; the full-height slide look (min-height + inner padding) is kept for the on-screen preview via `@media screen`. Fixes "content cut off across the board" and reduces sparse-then-data splits (usable height 255mm→265mm). Verified by rendering a 113-page real pack (project 5e275c28) — continuation pages now have consistent top margin + footer. File: `pdf_builder.py` PACK_CSS.
- 2026-06: Live readiness/completion sync — action-item endpoints (`confirm_item`, `update_action_item`, `add_action_item`, `delete_action_item`, `confirm_all_items`) now return `{itemsBeforeIssue, readiness, measures, completion}` via new `_project_snapshot()`; `get_project` sets `doc.completion` = avg of measure completions. Frontend `ActionItems` passes the snapshot up (`onItemsChange(items, r)`); `DesignWorkspace` IntelligencePanel handler + `ProjectOverview` confirm/confirm-all merge readiness/measures/completion into state → QA bar, overall % and Completion move instantly, no refresh. Tested (iter24) 100% backend, live-sync confirmed in UI.
- 2026-06: Photos library — sticky header (`photos-sticky-header`) with "Top" (`photos-back-to-top`, scrolls `#ws-scroll`) and "Save & Exit" (`photos-save-exit`, → project overview); curation grid now uses `thumbUrl` + lazy-load + fade-in on stable 4:3 boxes (was full-res `mediaUrl`) so photos load fast and cache. File: `DesignWorkspace.jsx` photos case.
- 2026-06: Design Pack "supercharge" — `start_pack_job` serves the cached PDF instantly (creates a done job pointing at `packPath`) when a pack was already built and `_pack_content_hash` still matches, skipping rebuild; `_office_to_pdf` caches LibreOffice xlsx/docx→PDF results in-process keyed by source-bytes hash so repeat builds skip re-conversion. Files: `server.py`, `pdf_builder.py`. (Instant-export path is code-verified; not runtime-tested to avoid slow full-gen in preview.)
- 2026-06: Readiness "why is this being asked?" tooltips — each Design Readiness row (Property Data, Measures, Specifications, Calculations, Junctions, Evidence, QA) now carries a `why` string built in `_compute_readiness` that names the measure(s) driving it. Evidence explicitly states whether loft-specific evidence is included ("because a loft/room-in-roof measure is in scope") or excluded ("no loft measure in scope"). Rendered as an Info-icon Radix tooltip per row in `ProjectOverview.jsx` (testid `readiness-why-<section>`; local TooltipProvider, click stopPropagation so it doesn't navigate). Verified live on a Solar+Vent (no-loft) project. 
- 2026-06: Readiness Evidence fix — loft-specific site-condition evidence (recessed downlights, loft cross-flow, loft storage, tank, ESH cable) is now only required when a loft/room-in-roof measure is in scope. Properties without loft insulation no longer get asked for recessed-spotlight evidence, and stale "completed but still outstanding" loft items clear. `_compute_readiness` in `server.py`. Verified: loft-measure projects still require it; non-loft projects exclude it.
- 2026-06: Bay windows — visual template palette (Box/Canted/Bow/Flat thumbnails) → click template, click plan to place (snaps to nearest wall), edit measurements/drag marker. Bays render as a double-line architectural symbol (outer wall + inner frame across facets) via `_bay_render` in both editor canvas and PDF (`cad_floorplan.py`, `FloorPlanGeometryEditor.jsx`). Verified: 3 inner-frame paths generated per test plan.
- 2026-06: Deleting a defect now sticks — site-note-derived defects add their key to `dismissedDefectKeys` so auto-match re-extraction never resurrects them (`delete_defect`, `_attach_sitenote_defect_photos`). Fixes "no dMEV in wet room (WC only)" recurring defect.
- 2026-06: Floor plan (`cad_floorplan.py`) — removed the RdSAP "I confirm… / Assessor-Operative signature / Date" confirmation block and stray page number from the generated plan; right-column divider + `VB_H` now re-flow to content (no empty band). No signatures in the design document.
- 2026-06: Appendix (`_collect_source_docs`) now binds ONLY product datasheets — vent/air-tightness/ADF1 docs are bound once in-section (Ventilation), fixing the duplicate copy at the bottom of the pack.
- 2026-06: PDF export fixes (mandatory docs + signatures + layout):
  - Removed the "Approval & Declaration" signature page from the design pack (no signatures in the design document). `pdf_builder.py` assembly.
  - In-section Ventilation now embeds EVERY matching uploaded doc (Ventilation Strategy / ADF1 Table D1 / Air-Tightness) exactly as provided — PDF, Excel, Word or image. Previously only PDFs embedded and Excel files were skipped (a generated D1 was substituted). `_bytes_to_page_uris` + reworked `_ventStrategyPages`.
  - xlsx print-prep (`_prep_xlsx_for_print`): fit-to-width + cleared headers/footers before LibreOffice conversion — fixes right-edge column clipping and removes the "in.xlsx - <date>" stamp. ADF1 D1 8→5pp, Air-Tightness 31→11pp, logos/tables intact. Verified against 5 Scudamore Place docs.
- 2026-06: Site Conditions now support MULTIPLE evidence photos per condition (mirrors defects). Evidence entries carry a `photos[]` array (primary = `url` for back-compat); multi-select picker (tap add/remove, sticky Done), removable thumbnail strip per condition. PDF card already renders the gallery (main + up to 9 thumbs). File: `SiteConditionsPanel.jsx` (no backend change — `save-site-conditions` stores verbatim, `_photos_data` already built).
- 2026-06: Design Sign-off UI card no longer shows a person's name — now reads "Design signed off · <date>". File: `DesignWorkspace.jsx` (PDF declaration page unchanged).
- 2026-06: Defects support MULTIPLE photos per defect. Backend append (deduped) on attach/upload, new `POST /defects/{id}/detach-photo`; multi-select picker; pack renders up to 3 per defect. Files: `server.py`, `DefectsPanel.jsx`, `pdf_builder.py`.
- 2026-06: ADF1 Table D1 checklist gained a "Non-compliant / required" status (red chip in pack + red verdict banner "resolve before sign-off"); flags ventilation summary on dashboard. Files: `VentilationPanel.jsx`, `pdf_builder.py`, `server.py`.
- 2026-06: Defects "From survey" gallery now loads the FULL photopack (getAllPhotos) and is grouped by area with sticky close, matching Site Conditions. Shared grouping util `lib/photoGroups.js`.
- 2026-06: Photo extraction thresholds (whiteness/flat/min-size) left relaxed per user — full access to every assessment photo. Verified.
- 2026-06: Site Conditions photo picker — click-off-to-close, sticky Close bar, grouped-by-area with "Other / unsorted" catch-all.

## Prioritized backlog
- P2 Slimming sliders: expose photos-per-measure and datasheet-page caps on Design Pack screen.
- P2 PDF page-spacing tidy pass (after user flags specific pages).
- P2 (post-launch) Monolith refactor: server.py (>3800), pdf_builder.py (>4800), ai_extractor.py (>2765). HOLD until after Monday go-live.

## Key files
- backend: server.py, ai_extractor.py, pdf_builder.py, cad_floorplan.py, cad_iso.py
- frontend: components/{SiteConditionsPanel,VentilationPanel,FloorPlanGeometryEditor,DocumentsList}.jsx, pages/DesignWorkspace.jsx, pages/workspace/*
- key endpoints: PUT /api/projects/{id}/ventilation, PUT /api/projects/{id}/floorplan, GET /api/projects/{id}/photos/all, GET /api/documents/{id}/embedded/{n}, DELETE /api/documents/{doc_id}, POST /api/projects/{id}/items/confirm-all

Test credentials: /app/memory/test_credentials.md
